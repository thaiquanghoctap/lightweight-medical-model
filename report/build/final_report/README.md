# Final report package

This folder is self-contained for submission.

- `main.tex`: main LaTeX source.
- `final_report.pdf`: compiled final report.
- `figures/`: all image files inserted in `main.tex`.

Build command from this folder:

```bash
tectonic main.tex
```
