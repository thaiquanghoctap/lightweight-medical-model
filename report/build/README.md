# Report build outputs

Build artifacts are grouped by document. Each document folder should contain:

- one source `.tex` snapshot;
- one generated `.pdf`;
- related LaTeX intermediate files only when they are useful for rebuilding or debugging.

Current groups:

- `final_report/`
- `research_progress_update/`
- `standalone_evidence_appendix/`
- `evidence_appendix/`
- `proposal/`
- `progress_update/`
